const styles = {
  container: {
    position: 'relative',
    // width: columns.length > 0 ? 300 * columns.length + 20 * columns.length : '100%',
    margin: '0 auto',
    height: '100vh',
    display: 'flex',
    gap: '20px',
    flexWrap: 'nowrap',
    justifyContent: 'start',
    paddingTop: '10rem',
    overflow: 'hidden',
  },
};

export default styles;
