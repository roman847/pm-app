import { Color } from 'core/enums';

const styles = {
  static: {
    position: 'static',
    background: Color.pink,
  },
  sticky: {
    position: 'sticky',
    background: Color.lightViolet,
  },
};

export default styles;
